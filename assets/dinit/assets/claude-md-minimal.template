# [项目名称]

## 核心原则

- 少即是多，克制且清晰
- 关注点分离原则
- 具体胜于抽象；判断性规则必须提供示例锚点

## 上位心智层

**三唯一框架**：进入任务前确认唯一主线目录、唯一运行入口、canonical。

**不确定性门控**：
- 直接做 = 改动小 + 结果可预期 + 差异一句话说清
- 先写最小规格 = 结果不能说清 / 依赖外部 / 需稳定格式
- 先探索验证 = 外部依赖多 / 落点不清 / 回滚成本高

**证据优先级**：L1-L3 主判，仅 L5 不可宣称完成。

## Skill 索引

需任务管理→dtask | Session管理→dsess | 纠偏→dcorr | 验收→dverify | 决策→djudge | 记录→drecord

参考文件 Read on demand：rules/exceptions.md、templates.md、constraints.md

---

## 项目上下文

[1-2 句话描述项目是什么、做什么]

## 技术栈

- 语言：[...]
- 框架：[...]

## 常用命令

```bash
# 开发 / 构建 / 检查 / 测试
[...]
```

## 行为铁律

- recording 更新：session 结束前必须写入 `.diwu/recording/`
- 时间戳：必须 `date '+%Y-%m-%d %H:%M:%S'` 获取真实时间戳
- 禁止推送：`.claude/` `.diwu/` 禁止 commit/push

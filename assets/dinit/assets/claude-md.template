# [项目名称]

## 项目上下文

- **用途**：[1-2 句话描述项目是什么、做什么]
- **功能摘要**：[一句话描述项目核心功能]

> 详细功能说明见项目根目录 README.md。详细的任务定义见 task.json

## 技术栈

- 语言：[TypeScript / Python / ...]
- 框架：[Next.js / FastAPI / ...]
- 样式：[Tailwind CSS / ...]
- 数据库：[PostgreSQL / Supabase / ...]

## 常用命令

```bash
# 开发
[npm run dev / python manage.py runserver / ...]

# 构建
[npm run build / ...]

# 检查
[npm run lint / ...]

# 测试
[npm run test / pytest / ...]
```

## 项目结构

```
/
├── .claude/
│   ├── CLAUDE.md          # 本文件
│   ├── task.json          # 任务定义（source of truth）
│   ├── recording/         # Session 进度记录目录
│   │   └── session-YYYY-MM-DD-HHMMSS.md
│   ├── checks/
│   │   └── smoke.sh       # 基线验证脚本
│   └── rules/
│       └── constraints.md # 架构约束（可选）
├── AGENTS.md              # 跨 AI 工具入口
├── init.sh                # 环境初始化脚本
└── [项目目录]/
    ├── src/
    └── ...
```

## 编码约定

- [约定1：例如 TypeScript strict mode]
- [约定2：例如 函数式组件 + hooks]
- [约定3：例如 遵循项目既有命名风格]

## 压缩指令

当压缩对话历史时，必须保留以下关键信息：
- 当前 InProgress 任务 ID、标题及完整 acceptance 条件
- Blocked 状态及具体阻塞原因和 blocked_by 任务 ID
- 待处理的变更请求（CR 编号、状态及建议方案）
- 当前 session 的未提交工作及验证结果

## 项目特定约束（可选）

如有架构级约束，在 `.claude/rules/constraints.md` 中定义。

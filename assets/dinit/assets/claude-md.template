# [项目名称]

## 工作流

本项目使用 diwu-workflow 插件，规则通过 Skill 渐进式披露按需加载。

## 核心不变量

### 三唯一框架
进入任务前确认：唯一主线目录、唯一运行入口、canonical（规范真相源）。

### 现象→判断→动作骨架
现象（事实）→ 判断（结论+依据）→ 动作（下一步+验证方式）

### 不确定性门控
| 路径 | 条件 |
|------|------|
| 直接做 | 改动小 + 结果可预期 + 一句话说清 |
| 最小规格 | 不能说清 / 依赖外部 / 需稳定格式 / 需交接 |
| 探索验证 | 外部依赖多 / 落点不清 / 回滚成本高 |

### 证据优先级
L1 运行态 > L2 调用链 > L3 自动化断言 > L4 表面观察 > L5 间接推断。L1-3 主判。

## Skill 与参考文件索引

| 名称 | 类型 | 触发场景 |
|------|------|---------|
| dtask | Skill | 任务管理、dtask、规划分解 |
| dsess | Skill | Session 生命周期、任务选择 |
| dcorr | Skill | 纠偏、误判排查、四行重写 |
| dverify | Skill | 验收判定、证据等级 |
| djudge | Skill | 阶段决策、幅度判定、并行选择 |
| drecord | Skill | session 记录、踩坑记录 |
| exceptions.md | 参考 | 异常处理与 BLOCKED |
| templates.md | 参考 | BLOCKED/REVIEW 格式 |
| constraints.md | 参考 | 五维约束设计 |

---

## 项目上下文

[1-2 句话描述项目是什么、做什么]

> 任务定义见 .diwu/dtask.json

## 技术栈

- 语言：[TypeScript / Python / ...]
- 框架：[Next.js / FastAPI / ...]
- 样式：[Tailwind CSS / ...]
- 数据库：[PostgreSQL / Supabase / ...]

## 常用命令

```bash
# 开发
[npm run dev / python manage.py runserver / ...]
# 构建
[npm run build / ...
# 检查
[npm run lint / ...
# 测试
[npm run test / pytest / ...]
```

## 项目结构

```
/
├── .claude/
│   ├── CLAUDE.md          # 本文件（v2 精简格式）
│   └── rules/
│       └── constraints.md # 项目特定约束（可选）
├── .diwu/
│   ├── dtask          # 任务定义
│   ├── recording/         # Session 记录
│   ├── checks/smoke.sh    # 基线验证
│   ├── archive/           # 归档
│   └── decisions.md       # 设计决策（可选）
├── AGENTS.md
├── init.sh
└── [项目目录]/
```

## 压缩指令

保留：当前 InProgress 任务 ID/title/acceptance、Blocked 状态及原因、未提交工作及验证结果。

## 编码约定

- [约定1]
- [约定2]
- [约定3]

## 子代理配置（可选）

<!-- 子代理并发数: 3 -->

## 项目特定约束（可选）

(仅在需要覆盖全局规则时填写)

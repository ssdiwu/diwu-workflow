<!-- project-pitfalls.md.template -->
<!--
  自动生成标注：此文件由 /dinit 从模板初始化生成。
  初始化后复制到用户项目的 .claude/project-pitfalls.md。
  用户可在生成的文件中追加本项目特有的高频误判条目。

  格式要求：
  - 每条误判使用四列表格（现象 / 默认先查 / 暂不下结论 / 何时下结论）
  - 与 pitfalls.md Layer 1 泛化模式格式一致
  - 按误判类型分组，每组加二级标题
-->

# 项目高频误判表

> **维护者**：项目团队（本地维护，不随插件更新覆盖）
> **来源**：pitfalls.md Layer 2 + 团队经验积累
> **消费场景**：出现异常时，先查 Layer 1 泛化模式 → 再查本表 → 再做判断

## 环境相关

| 现象 | 默认先查 | 暂不下结论 | 何时下结论 |
|------|---------|-----------|-----------|
| （示例：占位，请替换为本项目实际误判） | | | |

## 数据相关

| 现象 | 默认先查 | 暂不下结论 | 何时下结论 |
|------|---------|-----------|-----------|
| （示例：占位，请替换为本项目实际误判） | | | |

## 接口/契约相关

| 现象 | 默认先查 | 暂不下结论 | 何时下结论 |
|------|---------|-----------|-----------|
| （示例：占位，请替换为本项目实际误判） | | | |

## 验证相关

| 现象 | 默认先查 | 暂不下结论 | 何时下结论 |
|------|---------|-----------|-----------|
| （示例：占位，请替换为本项目实际误判） | | | |

## Session 2026-04-17 18:27:22
### Task#168: 用 hera-backend-demos 验证 /dinit 迁移能力并修正 → Done
**实施内容**:
- 继续收口 `commands/dinit.md` 的旧运行时迁移契约，明确要求“执行自动迁移”，避免只停留在提示层。
- 补强任务级测试 `tests/level3/test_task_168_dinit_runtime_migration_contract.py`：除文档契约断言外，新增基于 `subprocess.run(["claude", "-p", ...])` 的真实迁移测试，在临时副本中执行 `/dinit` 刷新流程。
- 以 `/Users/diwu/Documents/codes/Hera/hera-backend-demos/` 为基线来源，在隔离副本中验证 `.claude/` 运行时迁移到 `.diwu/`，并确认 `.claude/rules/constraints.md` 被刷新为通用版。
- 发现 `hera-backend-demos` 本身不存在 `.claude/skills/`，因此将真实验收落点收束为“Claude 原生目录如已存在则保留，不强制凭空创建”。
**验收验证**:
- [x] Given 在 hera-backend-demos 项目执行 /dinit 刷新 When 完成 Then .diwu/ 目录已创建且包含从 .claude/ 迁入的运行时文件（L1/L3: 隔离副本实测 + `pytest tests/level3/test_task_168_dinit_runtime_migration_contract.py`）
- [x] Given /dinit 完成后 When 检查 hera-backend-demos/.claude/ Then 不含运行时残留（task.json、recording/、decisions.md、project-pitfalls.md 已不在）；仍保留 Claude 原生目录（L1/L3: 隔离副本实测 + pytest）
- [x] Given hera-backend-demos/.diwu/task.json When 读取 Then 任务数据完整（与迁移前一致）（L1/L3: 隔离副本 JSON 校验 + pytest）
- [x] Given hera-backend-demos/.diwu/recording/ When 检查 Then 历史 session 文件完整（数量与迁移前一致）（L1/L3: 隔离副本 session 计数 + pytest）
- [x] Given /dinit 迁移逻辑 When 验证通过 Then 后续任何已有项目执行 /dinit 都能完成同样迁移（非硬编码特定项目）（L3: pytest 临时副本迁移测试）
### 下一步: 进入 Task#169，统一更新 0.8.0 版本号并做最终全量校验。
### 本次踩坑/经验
- [验证误读] `claude -p "/dinit ..."` 返回“已刷新”并不等于真实执行了迁移链路 → 根因是把命令回复文本误当成文件系统结果 → 错误判断是“文档契约测试通过就足以 Done” → 正确做法是增加临时副本实测，用 `.diwu/` 产物、旧路径消失、constraints 通用化三类可观测事实做验收。

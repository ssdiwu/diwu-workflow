## Session 2026-04-17 16:27:10
### Task#162: 移除 lessons.md 机制（全局抹除） → Done
**实施内容**:
- 删除 `hooks/scripts/user_prompt_submit.py` 中的 `lessons.md` 读取与注入逻辑。
- 删除 `commands/dinit.md` 中 `/dinit` 创建 `.claude/lessons.md` 的步骤与验证项。
- 删除模板文件 `assets/dinit/assets/lessons.md.template`。
- 新增 Task#162 专属测试 `tests/level3/test_task_162_remove_lessons.py`，避免被 163/164/166 的未来口径干扰。
**验收验证**:
- [x] Given hooks/scripts/user_prompt_submit.py When 读取 Then 不包含 lessons.md 的读取或注入逻辑（Read + pytest 断言）
- [x] Given commands/dinit.md When 读取 Then 不包含 lessons.md 的初始化或写入步骤（Read + pytest 断言）
- [x] Given assets/dinit/assets/ 目录 When 检查 Then 不存在 lessons.template 文件（pytest 断言）
- [x] Given 运行 python3 hooks/scripts/user_prompt_submit.py （模拟）Then 输出 additionalSystemPrompt 中不包含 'Agent 错误模式记录' 或 'lessons' 字样（pytest 断言）
- [x] `python3 -m py_compile hooks/scripts/user_prompt_submit.py`（L3）
- [x] `python3 -m pytest tests/level3/test_task_162_remove_lessons.py` → 4 passed（L3）
### 下一步: 进入 Task#163，拆分 constraints.md 并把注入降级到索引层。
### 本次踩坑/经验
- [验证误读] 直接跑现有 level3 测试会提前命中 163/164/166 的未来要求 → 误把“总测试失败”当成“Task#162 未完成” → 正确做法是给当前任务补独立测试文件，只验证本任务 acceptance。

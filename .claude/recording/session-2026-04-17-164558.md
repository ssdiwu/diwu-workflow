## Session 2026-04-17 16:45:58
### Task#163: constraints.md 拆分——通用保留 + 插件专属迁 CLAUDE.md + 注入降级 → Done
**实施内容**:
- 对齐 `assets/dinit/assets/rules/constraints.md` 与 `.claude/rules/constraints.md`，将模板收口为纯通用版架构约束。
- 保持 `hooks/scripts/user_prompt_submit.py` 中 `constraints.md` 仅走速查索引注入，不再全文注入。
- 保持 `.claude/CLAUDE.md` 中插件专属约束章节，用于承接版本号语义化、发版状态机、跨平台路径、并发约束、数据所有权。
- 新增并执行 Task#163 专属测试 `tests/level3/test_task_163_constraints_split.py`，只验证本任务 acceptance。
**验收验证**:
- [x] Given `.claude/rules/constraints.md` When 读取 Then 标题为通用架构约束且不含插件专属章节（Read + pytest 断言）
- [x] Given `.claude/CLAUDE.md` When 读取 Then 保留插件专属约束章节（Read + pytest 断言）
- [x] Given `hooks/scripts/user_prompt_submit.py` When 读取 Then `constraints.md` 不再独立全文注入且出现在 rfs 索引（Read + pytest 断言）
- [x] Given `assets/dinit/assets/rules/constraints.md` When 读取 Then 与项目规则文件内容一致（pytest 断言）
- [x] `python3 -m py_compile hooks/scripts/user_prompt_submit.py`（L3）
- [x] `python3 -m pytest tests/level3/test_task_163_constraints_split.py` → 5 passed（L3）
### 下一步: 进入 Task#164，补 `mindset.md` 的 project-pitfalls 必读指令并更新速查索引。
### 本次踩坑/经验
- [验证误读] 看到项目规则文件已是通用版就容易误判 Task#163 已完成 → 实际缺口在 `/dinit` 模板仍残留旧插件专属版本 → 正确做法是用任务级测试同时校验项目规则、模板、注入脚本和 CLAUDE.md 四处一致性。

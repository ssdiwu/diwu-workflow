## Session 2026-04-17 16:57:31
### Task#164: 注入策略优化——mindset.md 追加 project-pitfalls 必读指令 + 速查索引补全 → Done
**实施内容**:
- 保持 `.claude/rules/mindset.md` 中的“误判表预加载（必须执行）”段落，要求在 `.diwu/project-pitfalls.md` 存在时主动读取完整内容。
- 更新 `hooks/scripts/user_prompt_submit.py`，在速查索引中追加 `project-pitfalls.md` 条目，并让该条目从 `.diwu/project-pitfalls.md` 判定是否存在。
- 新增 Task#164 专属测试 `tests/level3/test_task_164_project_pitfalls_index.py`，覆盖规则文案、索引条目与运行态输出三类断言。
**验收验证**:
- [x] Given `.claude/rules/mindset.md` When 读取 Then 包含 “### 误判表预加载（必须执行）” 且要求读取 `.diwu/project-pitfalls.md`（Read + pytest 断言）
- [x] Given `hooks/scripts/user_prompt_submit.py` When 读取 Then rfs 速查索引包含 `('project-pitfalls.md', '项目高频误判表（Layer 2）')`（Read + pytest 断言）
- [x] Given `.diwu/project-pitfalls.md` 存在 When 运行 `user_prompt_submit.py` Then 输出索引包含 `project-pitfalls.md` 条目（pytest 临时目录断言）
- [x] `python3 -m py_compile hooks/scripts/user_prompt_submit.py`（L3）
- [x] `python3 -m pytest tests/level3/test_task_164_project_pitfalls_index.py` → 3 passed（L3）
### 下一步: 进入 Task#165，开始把 hook 脚本中的运行时路径从 `.claude/` 迁到 `.diwu/`。
### 本次踩坑/经验
- [分层未拆清] 容易把 `project-pitfalls.md` 当成 rules 目录里的普通规则文件处理 → 实际它属于 `.diwu/` 运行时文件，不该走 `.claude/rules/` 的存在性检查 → 正确做法是为该条目单独判定存在路径，并用任务级测试覆盖运行态索引输出。

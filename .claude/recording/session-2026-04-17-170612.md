## Session 2026-04-17 17:06:12
### Task#165: Hook 脚本路径迁移——.claude/ → .diwu/ 批量替换 → Done
**实施内容**:
- 将 12 个 hook 脚本中的运行时路径从 `.claude/` 批量迁移到 `.diwu/`，覆盖 `task.json`、`recording/`、`decisions.md`、`dsettings.json`、`project-pitfalls.md`。
- 将 `hooks/scripts/task_created_validate.py` 的 `TASK_JSON_PATH` 从硬编码绝对路径改为相对路径 `.diwu/task.json`。
- 新增 Task#165 专属测试 `tests/level3/test_task_165_hook_paths_to_diwu.py`，覆盖路径迁移、零残留、相对路径与 `py_compile` 四类断言。
- 顺手补齐 `.claude/task.json` 中 Task#163、Task#164 的 Done 状态一致性，确保历史任务状态与已提交记录一致。
**验收验证**:
- [x] Given hooks/scripts/ 下所有 .py 文件 When grep '\.claude/(task\.json|recording|decisions|dsettings|project-pitfalls|archive)' Then 匹配结果为空（Grep 零匹配）
- [x] Given hooks/scripts/task_created_validate.py When 读取 Then TASK_JSON_PATH 为相对路径 '.diwu/task.json'（pytest 断言）
- [x] Given hooks/scripts/post_tool_reminder.py When 读取 Then 引用文本中 .diwu/ 路径正确（pytest 断言）
- [x] Given 所有 hook 脚本 When py_compile Then 全部通过（`python3 -m py_compile hooks/scripts/*.py`）
- [x] `python3 -m pytest tests/level3/test_task_165_hook_paths_to_diwu.py` → 4 passed（L3）
### 下一步: 进入 Task#166，更新 /dinit 命令与模板中的运行时目录到 `.diwu/`，并补 Task#166 专属测试。
### 本次踩坑/经验
- [验证误读] 只看 git commit message 容易误判 task.json 已经同步为 Done → 之前提交里 task.json 未必真的随任务一起落盘 → 正确做法是先直接读取 task.json 核对状态，再进入下一任务。
